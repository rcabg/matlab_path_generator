%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%% Matrices y variables %%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

t_r=-1*ones(mat_per_max,Nmax);
t_c=-1*ones(mat_per_max,Nmax);

t_r_ini=-1*ones(mat_per_max,1);
t_c_ini=-1*ones(mat_per_max,1);

t_r_end=-1*ones(mat_per_max,1);
t_c_end=-1*ones(mat_per_max,1);

xt_r=-1*ones(mat_per_max,Nmax);
yt_r=-1*ones(mat_per_max,Nmax);

xt_c=-1*ones(mat_per_max,Nmax);
yt_c=-1*ones(mat_per_max,Nmax);

id_c=-1*ones(mat_per_max);
id_r=-1*ones(mat_per_max);

jj=1;
ii_id_c=1;
ii_id_r=1;
cont_intruso=0;